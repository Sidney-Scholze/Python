print('\n| Converte Real para dolar |')
cotacao = float(input('Qual o valor do dolar agora ? : '))
real = float(input('Quantos reais voce tem ? : '))
print('Voce pode comprar {:.2f} dolar(es)' .format (real / cotacao ))
